- content_for :angular_app do
  cartShirtsApp

- content_for :angular_controller do
  CartShirtsCtrl

%section
  .container.checkout.clearfix
    .checkout-inner
      = render 'steps', step: 1
    %table.cart-shirts
      %thead
        %tr
          %td.name Product
          %td Price
          %td Color
          %td Size
          %td.quantity Quantity
          %td.total Total
          %td.trash
      %tbody
        %tr{ 'ng-repeat' => 'shirt in cart.cart_shirts' }
          %td
            %i.icon-tshirt
            {{ shirt.name }}
          %td {{ shirt.price | currency }}
          %td
            .colors
              .color
                %div{ 'ng-style' => '{"background-color": shirt.hex_value}' }
          %td
            .sizes
              %span {{ shirt.size }}
          %td
            %input.medium{ type: 'text', 'ng-model' => 'shirt.quantity', 'text-blur' => 'update_shirt(shirt)', 'enter-pressed' => 'blur', 'ng-class' => '{ error: errors[shirt.id].quantity }' }
            .alert.error{ 'ng-show' => 'errors[shirt.id].quantity' }
              %button.close{ 'hide-alert' => 'hide_shirt_alert(shirt, "quantity")', 'eat-click' => '' } &times;
              {{ errors[shirt.id].quantity }}
          %td.total {{ total(shirt) | currency }}
          %td.trash
            = link_to '', 'fade-out' => 'delete(shirt)' do
              %i.icon-trash
    .checkout-inner
      .cart-total.pull-right
        %h3.inline Total
        %span {{ cart.total | currency }}
    .submit.clear
      = link_to '&larr; Back to shopping'.html_safe, order_path, class: 'button grey'
      = link_to 'Checkout &rarr;'.html_safe, checkout_path, class: 'button pull-right red'

:javascript
  $(document).ready(function() {
    AngularDataSeed({
      cart: #{render partial: 'carts/cart', locals: { cart: current_cart }, formats: [:json]}
    });
  });